apiVersion: apps/v1
kind: Deployment
metadata:
  name: cd-pipeline-handler-controller-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      name: cd-pipeline-handler-controller-v2
  template:
    metadata:
      labels:
        name: cd-pipeline-handler-controller-v2
    spec:
      serviceAccountName: edp
      containers:
        - name: cd-pipeline-handler-controller-v2
          # Replace this with the built image name
          image: REPLACE_IMAGE
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              value: REPLACE_VALUE
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "cd-pipeline-handler-controller-v2"
